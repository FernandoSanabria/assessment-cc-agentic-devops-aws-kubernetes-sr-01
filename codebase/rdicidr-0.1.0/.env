# Baked into the client bundle at build time by Create React App.
# REACT_APP_* values are public by definition -- never put secrets here.
# Machine-local overrides belong in .env.local, which stays gitignored.
REACT_APP_API_URL=https://api.rdicidr.com
